function variable = QThree_Nouh(x,y)

    if x>=0
        if y>=0
    variable = (x+y);

     else
    variable = (x(y^2));

    end 
else
   
    if y>=0
        variable = (x^2)+y;
    else 
        variable = (x^2)+(y^2);
    end
end

